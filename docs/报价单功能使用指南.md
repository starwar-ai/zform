# 报价单功能使用指南

## 概述

报价单功能已完全集成到 zform 的统一单据框架中。所有的 CRUD 操作、审批流程、柜型计算等业务逻辑均已实现。

## ✅ 已实现功能

### 后端功能
- ✅ 数据模型（Quotation + QuotationItem）
- ✅ 柜型自动计算与验证（20尺、40尺、40尺高柜、散货）
- ✅ 审批流程集成
- ✅ 统一文档 API（`/api/documents/quotation/*`）
- ✅ 自定义操作（提交、撤回、关闭、接受、打印等）

### 前端功能
- ✅ Schema 定义（主数据 + 明细表）
- ✅ 菜单注册（业务入口 → 报价单）
- ✅ 权限配置（CRUD + 审批权限）
- ✅ 操作按钮配置（列表操作 + 表单操作）
- ✅ 自动表单渲染
- ✅ 柜型计算辅助组件（可选）

## 使用方式

### 1. 访问报价单

登录系统后，在左侧菜单中找到：
```
业务入口 → 报价单
```

### 2. 创建报价单

1. 点击工具栏的"新建"按钮
2. 填写主数据：
   - 公司主体
   - 客户名称
   - 价格条款
   - 客户联系人
   - 币种
   - 出运国家
   - 出运口岸
   - 有效期止

3. 添加报价明细：
   - 产品编号
   - 产品名称（中英文）
   - 规格
   - 报价
   - 供应商信息
   - 包装信息（箱数、外箱装量、外箱体积等）
   - **柜型信息**（20尺柜、40尺柜、40尺高柜、散货）

4. 保存或提交审批

### 3. 柜型计算

#### 自动计算（后端）
在保存/更新报价单时，系统会自动验证柜型数据是否与箱数和体积匹配。

#### 手动计算（前端辅助）
如果需要辅助计算，可以使用 `CabinetCalculator` 组件：

```tsx
import { CabinetCalculator } from "@/components/quotation-cabinet-calculator"

// 在明细表上方添加计算按钮
<CabinetCalculator 
  items={detailTableData}
  onCalculated={(results) => {
    // 将计算结果更新到明细表
    results.forEach((result, index) => {
      updateDetailRow(index, {
        container20ft: result.container20ft,
        container40ft: result.container40ft,
        container40hq: result.container40hq,
        bulkCargo: result.bulkCargo,
      })
    })
  }}
/>
```

#### 计算规则
- **20尺柜标准容量**：28 m³
- **40尺柜标准容量**：58 m³
- **40尺高柜标准容量**：68 m³
- **优先顺序**：40尺高柜 → 40尺柜 → 20尺柜 → 散货
- 不足一个柜的余量计入散货

### 4. 审批流程

报价单状态流转：
```
草稿 → 已提交 → 已审批 → 已接受
  ↓      ↓
已取消  已拒绝
```

可用操作：
- **保存**：草稿状态下可用
- **提交**：将报价单提交审批
- **审批/拒绝**：有审批权限的用户可用
- **撤回**：提交者可撤回待审批的单据
- **关闭**：关闭已审批的报价单
- **作废**：作废已审批的报价单

### 5. 生成销售合同

审批通过后，可以点击"生成销售合同"按钮，基于报价单创建销售合同。

## API 端点

所有操作通过统一的文档 API：

```
GET    /api/documents/quotation/list                  # 列表查询
POST   /api/documents/quotation                       # 创建
GET    /api/documents/quotation/:id                   # 详情
PUT    /api/documents/quotation/:id                   # 更新
DELETE /api/documents/quotation/:id                   # 删除
POST   /api/documents/quotation/:id/actions/:action   # 自定义操作
```

### 自定义操作
```bash
# 提交审批
POST /api/documents/quotation/:id/actions/submit

# 撤回
POST /api/documents/quotation/:id/actions/withdraw

# 关闭
POST /api/documents/quotation/:id/actions/finish

# 接受
POST /api/documents/quotation/:id/actions/accept

# 打印
POST /api/documents/quotation/:id/actions/print

# 转销售合同
POST /api/documents/quotation/:id/actions/toSalesContract

# 计算柜型
POST /api/documents/quotation/:id/actions/calculateContainers
Body: { items: [...] }
```

## 数据库表

### Quotation（报价单主表）
```prisma
model Quotation {
  id                  String            @id @default(uuid())
  code                String            @unique          // 报价单号
  status              QuotationStatus                    // 状态
  approvalStatus      ApprovalStatus                     // 审批状态
  printStatus         PrintStatus                        // 打印状态
  
  // 客户信息
  customerId          String
  customerCode        String
  customerName        String?
  isNewCustomer       Boolean           @default(false)
  customerContactId   String?
  customerContactName String?
  
  // 国家信息
  countryId           String?
  countryName         String?
  
  // 内部法人单位
  internalCompanyId   String?
  internalCompanyName String?
  
  // 出运口岸
  departurePortId     String?
  departurePortName   String?
  
  // 交易信息
  currency            String
  priceTerms          String?
  validUntil          DateTime?
  
  // 人员信息
  salesPerson         String
  
  // 流程信息
  processInstanceId   String?
  
  // 明细行
  items               QuotationItem[]
  
  // 审计字段
  createdBy           String
  createdAt           DateTime          @default(now())
  updatedBy           String?
  updatedAt           DateTime          @updatedAt
  deletedAt           DateTime?
}
```

### QuotationItem（报价单明细表）
```prisma
model QuotationItem {
  id                    String      @id @default(uuid())
  quotationId           String
  quotation             Quotation   @relation(...)
  
  lineNumber            Int                               // 行号
  
  // 产品信息
  productCode           String?
  productNameCn         String
  productNameEn         String?
  spec                  String?
  productSpec           String?
  productImage          String?
  baseProductCode       String?
  customerProductNo     String?
  
  // 产品标记
  isSelfOwnedProduct    Boolean     @default(false)
  isCustomerProduct     Boolean     @default(false)
  isSeparateContainer   Boolean     @default(false)
  
  // 价格信息
  unitPrice             Decimal
  taxUnitPrice          Decimal?
  minOrderQuantity      Int?
  commissionRate        Decimal?
  
  // 供应商信息
  supplierId            String?
  supplierCode          String?
  supplierName          String?
  
  // 柜型信息
  bulkCargo             Decimal?
  container20ft         Int?
  container40ft         Int?
  container40hq         Int?
  
  // 包装信息
  packageMethod         String?
  innerBoxQty           Int?
  outerBoxQty           Int?
  boxCount              Int?
  outerBoxUnit          String?
  outerBoxLength        Decimal?
  outerBoxWidth         Decimal?
  outerBoxHeight        Decimal?
  outerBoxVolume        Decimal?
  outerBoxNetWeight     Decimal?
  outerBoxGrossWeight   Decimal?
  
  // 产品描述
  productDescription    String?
  productDescriptionEn  String?
  hsCode                String?
  deliveryDate          DateTime?
}
```

## 权限配置

### 菜单权限
- `quotation:query` - 查看报价单列表
- `quotation:detail` - 查看报价单详情

### 操作权限
- `quotation:create` - 创建报价单
- `quotation:update` - 更新报价单
- `quotation:delete` - 删除报价单
- `quotation:submit` - 提交审批
- `quotation:approve` - 审批
- `quotation:close` - 关闭
- `quotation:void` - 作废
- `quotation:push_down` - 生成销售合同

## 技术架构

### 前端
- **Schema 驱动**：通过 `quotation-schemas.ts` 定义表单结构
- **统一渲染**：`document-form.tsx` 自动渲染表单
- **统一 API**：`document-api.ts` 提供标准化接口

### 后端
- **Adapter 模式**：`quotation.adapter.ts` 定义业务逻辑
- **生命周期钩子**：`onCreate`、`onUpdate`、`beforeDelete`
- **自定义操作**：`actions` 定义特定业务操作
- **审批集成**：通过 `ApprovalService` 统一处理

## 迁移对比

| 功能 | zexport (Vue) | zform (React) | 说明 |
|------|---------------|---------------|------|
| 表单定义 | 手写每个字段 | Schema 声明 | zform 更简洁 |
| CRUD 操作 | 自定义 API | 统一 API | zform 标准化 |
| 审批流程 | 独立实现 | 框架集成 | zform 开箱即用 |
| 柜型计算 | 前端计算 | 后端验证 | zform 更可靠 |
| 代码量 | ~2000 行 | ~600 行 | zform 减少 70% |

## 下一步优化（可选）

1. **产品选择组件**
   - 创建通用的产品选择对话框
   - 支持多选、筛选、预览

2. **打印模板**
   - 配置报价单打印模板
   - 支持多种格式（PDF、Excel）

3. **导出功能**
   - 导出为 Excel
   - 支持自定义列

4. **批量操作**
   - 批量提交
   - 批量审批
   - 批量导出

## 常见问题

### Q: 如何修改柜型计算规则？
A: 修改 `e:\zform\server\src\documents\adapters\quotation.adapter.ts` 中的 `calcCabinetNum` 函数。

### Q: 如何添加新的自定义操作？
A: 在 adapter 的 `actions` 对象中添加新的操作函数。

### Q: 如何修改表单字段？
A: 修改 `e:\zform\src\schemas\quotation-schemas.ts` 中的 `masterFields` 或 `detailTables`。

### Q: 如何配置审批流程？
A: 通过菜单"业务配置 → 审批流程管理"配置报价单的审批规则。

## 总结

报价单功能已经完全迁移并集成到 zform 框架中，提供了比原 zexport 系统更加简洁、标准化的实现。所有核心业务逻辑（柜型计算、审批流程、数据验证）都已在后端实现，前端通过 Schema 驱动自动渲染，大大提高了开发效率和代码可维护性。
